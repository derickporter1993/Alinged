# Server
PORT=3001
NODE_ENV=production

# Database (SQLite file path)
DB_PATH=/data/hivemind.db

# Authentication
JWT_SECRET=change-this-to-a-random-secret

# CORS (set to your domain in production, or * for any origin)
CORS_ORIGIN=*
